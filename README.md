# SpringBoot-SPG-and-Eureka-Server
Hands on working with springboot dependencies like spring cloud gateway and service discovery like eureka server

Client
 ↓
Keycloak (Authentication only)
 ↓
Auth Service (Org resolution + App JWT)
 ↓
API Gateway (SCG)
 ↓
User / Product Services
 ↓
PostgreSQL (Schema-based MT)

